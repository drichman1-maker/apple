// Shared cross-workspace types live here. Add exports as the need arises.
// Consumers: `import type { ... } from '@repo/types'` after adding to
// dependencies / devDependencies in the consuming workspace.
export {};
